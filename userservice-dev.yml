spring:
  datasource:
    url: jdbc:mysql://localhost/test_db
    username: root
    password: ps123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  application:
    name: userservice
server:
  port: 8883
eureka:
  instance:
    preferIpAddress: true
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://192.168.71.131:8761/eureka/
